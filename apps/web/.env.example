# API URL (no trailing slash). Leave empty to use the page origin (/api — best with a single ngrok tunnel on Vite).
# VITE_API_URL=
#
# Two tunnels (separate API): point to the public API host.
# VITE_API_URL=https://tunnel-api.ngrok-free.app
